# Ghostscript UI Philosophy

Ghostscript UI streamlines document conversion. The web interface should remain thin while a containerized backend handles Ghostscript operations. Batch tasks must be scriptable and avoid proprietary formats. The goal is a straightforward tool that non-technical users can operate without complex setup.
