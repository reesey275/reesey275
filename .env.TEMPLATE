# ============================================================================
# Environment Configuration Template - reesey275 (Monorepo)
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to `.env` in your working directory
# 2. Fill in your actual configuration values
# 3. Never commit `.env` to version control
# 4. For CI/deployment, use environment variables or a secure vault
#
# NOTE: This is a monorepo. Subprojects may have their own .env.TEMPLATE files.
#

# ============================================================================
# ENVIRONMENT & RUNTIME
# ============================================================================

# Global environment mode
# Options: development, staging, production
ENVIRONMENT=development

# Enable debug/verbose logging across the monorepo
DEBUG=false

# Log level (if applicable)
# Options: debug, info, warn, error
LOG_LEVEL=info
